<%# <p style="color: green"><%= notice %>
<div class="p-3">
<%= link_to content_tag(:i, '', class: 'fas fa-arrow-left'), :back, class: "btn btn-outline-dark border-0 mb-3" %>
<%= render @tour if current_user.organisateur %>
</div>
