
<div class="sidebar bg-light  w-100 px-4" style="position: sticky; top: 90px; height: 90vh;">
  <% sidebar_links.each do |link| %>
    <%= link_to link[:label], link[:url], class: "nav-link mb-3 pt-3 " %>
  <% end %>
</div>
