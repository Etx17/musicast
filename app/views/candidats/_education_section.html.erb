<div class="p-3 bg-white rounded-4 d-flex flex-column border mt-3">
  <div class="d-flex justify-content-start align-items-start gap-3 mb-3">
    <h2>Formation</h2>
    <%= link_to edit_candidat_path, class:"btn btn-light rounded-circle p-0" do %>
      <i class="fas fa-pencil rounded-circle shadow text-secondary p-2" ></i>
    <% end %>
  </div>
  <% if @candidat.educations.none? %>
    <p class="text-muted">Aucune expérience renseignée</p>
  <% end %>
  <ul class="list-unstyled">
    <% @candidat.educations.each do |education| %>
      <li class="">
        <div class="d-flex align-items-between">
          <div class="d-flex w-100">
            <img src="https://source.unsplash.com/random" alt="Company Logo" class="me-3" style="width: 50px; height: 50px;"> <!-- Placeholder for company logo -->
            <div>
              <h3 class="font-semibold"><%= education.title %></h3>
              <p class="text-muted m-0"><%= education.description %></p>
              <p class="text-muted m-0"><%= education.start_date&.strftime('%b %Y') %> - <%= education.end_date.present? ? education.end_date&.strftime('%b %Y') : 'Present' %></p>
            </div>
          </div>
          <%= button_to candidat_education_path(@candidat, education), method: :delete, data: {turbo_confirm: "Supprimer cette expérience ?"}, class: 'btn btn-primary  border-0' do %>
            <i class="fas fa-times"></i>
          <% end %>
        </div>
        <% unless education == @candidat.educations.last %>
          <hr class="my-3" />
        <% end %>
      </li>
    <% end %>
  </ul>

</div>

</div>
