<div class="container pt-5">
  <h1 class="text-center mb-5"><%= t('imposed_works.new.title') %></h1>

  <%= render "form", imposed_work: @imposed_work %>

  <div class="text-center mt-4">
    <%= link_to t('imposed_works.common.actions.back'), organism_competition_edition_competition_category_path(@organism, @competition, @edition_competition, @category), class: 'btn btn-outline-dark' %>
  </div>
</div>
