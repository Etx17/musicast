<h1 class="ps-3 mb-0"><%= t('imposed_works.edit.title') %> / <%= @imposed_work.category.name %></h1>

<%= render Navigation::BackLinkComponent.new(
  link_path: organism_competition_edition_competition_category_path(@organism, @competition, @edition_competition, @category),
  link_text: @imposed_work.category.name)
%>

<div class="container">
  <%= render 'form', imposed_work: @imposed_work %>
</div>
