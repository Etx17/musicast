<div class="container">
  <%# Button deretour  %>
  <%= link_to t('imposed_works.edit.back_button'), :back, class: 'btn btn-secondary' %>

  <h1><%= t('imposed_works.edit.title') %></h1>

  <div class="text-center text-white rounded-pill bg-info mb-5 w-50 mx-auto d-block" style="font-size: 3em;"><%= @imposed_work.category.name %></div>
  <%= render 'form', imposed_work: @imposed_work %>
</div>
