<div class="container mt-5">
  <h1 class="text-center" style="font-size: 5em;"><%=  @edition_competition.annee%></h1>
  <div class="row">
    <div class="col-md-6" data-controller="hover">
      <h2 class="text-center text-dark mb-5" style="font-size: 4em;">Categories</h2>
      <% @edition_competition.categories.each do |category| %>
        <%= link_to category_path(category), data: { controller: "hover" }  do %>
          <div class="card mt-3" style="width: 100%; height: 300px; background-image: url('https://source.unsplash.com/random/?opera/300/200'); background-size: cover;" data-action="mouseenter->hover#hover mouseleave->hover#unhover">
            <div class="card-img-overlay d-flex justify-content-center align-items-center" style="background-color: rgba(0, 0, 0, 0.5);">
              <h2 class="card-title text-white" style="font-size: 3em;" data-hover-target="text"><%= category.name %></h2>
            </div>
          </div>
        <% end %>
      <% end %>

      <%= link_to 'Add Category', new_competition_edition_competition_category_path(@competition, @edition_competition), class: 'btn btn-dark w-100 mt-4' %>
    </div>

    <!--div class="col-md-6">
      <h2 class="text-center mb-5 text-dark" style="font-size: 4em;">Edition</h2>
      <div class="card border-0 shadow-sm rounded bg-grey">
        <div class="card-body">
          <p class="text-success"><%= notice %></p>
          <%= render @edition_competition %>
          <div class="mt-3 d-flex justify-content-between">
            <%= link_to "Edit this edition competition", edit_edition_competition_path(@edition_competition), class: 'btn btn-outline-dark' %>
            <%= button_to "Destroy this edition competition", @edition_competition, method: :delete, class: 'btn btn-outline-dark', data: { confirm: 'Are you sure?' } %>
          </div>
        </div>
      </div>
    </div-->
    <div class="col-md-6">
      <h2 class="text-center mb-5 text-dark" style="font-size: 4em;">Edition</h2>
      <div class="card border-0 shadow-sm rounded bg-grey">
        <div class="card-body">
          <p class="text-success"><%= notice %></p>
          <%= render @edition_competition %>
          <div class="mt-3 d-flex justify-content-between">
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
