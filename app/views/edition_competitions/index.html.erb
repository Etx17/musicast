<p style="color: green"><%= notice %></p>

<h1><%= t('edition_competitions.index.title') %></h1>

<div id="edition_competitions">
  <% @edition_competitions.each do |edition_competition| %>
    <%= render edition_competition %>
    <p>
      <%= link_to t('edition_competitions.index.show'), edition_competition %>
    </p>
  <% end %>
</div>

<%= link_to t('edition_competitions.index.new'), new_edition_competition_path %>
