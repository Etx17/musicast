<div class="container">
  <p style="color: green"><%= notice %></p>
  <div class="d-flex flex-column">

    <%= render @inscription %>
    <%# render le programme pas editable si c'est payé %>
    <div class="d-flex justify-content-between align-items-center">
      <%= link_to "Edit this inscription", edit_inscription_path(@inscription), class: "btn btn-outline-dark" %>
      <%= link_to "Back to inscriptions", inscriptions_path, class: "btn btn-outline-dark" %>

      <%= button_to "Destroy this inscription", @inscription, method: :delete, data: { turbo_confirm: 'Are you sure?' }, class: "btn btn-danger" %>
    </div>
  </div>
</div>
