<p style="color: green"><%= notice %></p>

<h1><%= t('airs.index.title') %></h1>

<div id="airs">
  <% @airs.each do |air| %>
    <%= render air %>
    <p>
      <%= link_to t('airs.common.actions.show'), air %>
    </p>
  <% end %>
</div>

<%= link_to t('airs.common.actions.new'), new_air_path %>
